server:
  port: 8080
  servlet:
    encoding:
      charset: utf-8
      force-response: true

spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      file-size-threshold: 1MB
      max-file-size: 100MB
      max-request-size: 100MB
  profiles:
    active: outer